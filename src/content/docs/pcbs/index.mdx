---
title: HHKB PCBs
description: Complete list of HHKB PCBs and their specifications
---

import { db, Pcbs } from 'astro:db';

export const pcbs = await db.select().from(Pcbs);

# HHKB PCBs

Below is a complete list of HHKB PCBs in our database.

{pcbs.map((pcb) => (
  <div key={pcb.id} style="border: 1px solid #ccc; padding: 1rem; margin: 1rem 0; border-radius: 8px;">
    <h2>{pcb.name}</h2>
    <ul>
      <li><strong>Layout:</strong> {pcb.layout}</li>
      <li><strong>Manufacturer:</strong> {pcb.manufacturer || 'N/A'}</li>
      <li><strong>Bluetooth:</strong> {pcb.supports_bluetooth ? 'Yes' : 'No'}</li>
      <li><strong>Connector:</strong> {pcb.connector_type || 'N/A'}</li>
      {pcb.release_date && <li><strong>Release Date:</strong> {new Date(pcb.release_date).toLocaleDateString()}</li>}
      {pcb.notes && <li><strong>Notes:</strong> {pcb.notes}</li>}
    </ul>
  </div>
))}
